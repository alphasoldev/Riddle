<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>R I D D L E</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');
        
        :root {
            --main-bg: #000000;
            --text-color: #00ff41;
            --accent-color: #ff0044;
            --secondary-color: #005890;
            --terminal-bg: #0a0a0a;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Share Tech Mono', monospace;
        }
        
        body {
            background-color: var(--main-bg);
            color: var(--text-color);
            overflow-x: hidden;
            line-height: 1.6;
        }
        
        .noise {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
            opacity: 0.05;
            pointer-events: none;
            z-index: 100;
        }
        
        .scanline {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, rgba(0,0,0,0), rgba(0,0,0,0) 50%, rgba(0,255,65,0.05) 50%, rgba(0,0,0,0));
            background-size: 100% 4px;
            z-index: 101;
            pointer-events: none;
            animation: scanline 6s linear infinite;
        }
        
        @keyframes scanline {
            0% {
                background-position: 0 0;
            }
            100% {
                background-position: 0 100%;
            }
        }
        
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 10;
        }
        
        header {
            padding: 20px 0;
            text-align: center;
            margin-bottom: 40px;
            border-bottom: 1px solid rgba(0, 255, 65, 0.3);
            position: relative;
        }
        
        .logo {
            font-size: 3.5em;
            letter-spacing: 15px;
            margin-bottom: 10px;
            display: inline-block;
            position: relative;
            text-shadow: 0 0 10px var(--text-color);
        }
        
        .logo::before {
            content: "R I D D L E";
            position: absolute;
            top: 0;
            left: 0;
            color: rgba(255, 0, 68, 0.4);
            animation: glitch 2s infinite;
        }
        
        .logo::after {
            content: "R I D D L E";
            position: absolute;
            top: 0;
            left: 0;
            color: rgba(0, 88, 144, 0.4);
            animation: glitch 3s infinite;
            animation-delay: 0.5s;
        }
        
        @keyframes glitch {
            0% {
                transform: translate(0);
            }
            20% {
                transform: translate(-2px, 2px);
            }
            40% {
                transform: translate(-2px, -2px);
            }
            60% {
                transform: translate(2px, 2px);
            }
            80% {
                transform: translate(2px, -2px);
            }
            100% {
                transform: translate(0);
            }
        }
        
        .tagline {
            font-size: 1em;
            letter-spacing: 3px;
            color: rgba(0, 255, 65, 0.7);
            margin-bottom: 15px;
        }
        
        .flicker {
            animation: flicker 3s linear infinite;
        }
        
        @keyframes flicker {
            0%, 19.999%, 22%, 62.999%, 64%, 64.999%, 70%, 100% {
                opacity: 1;
            }
            20%, 21.999%, 63%, 63.999%, 65%, 69.999% {
                opacity: 0.5;
            }
        }
        
        .matrix-effect {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
            pointer-events: none;
        }
        
        .terminal {
            background-color: var(--terminal-bg);
            border: 1px solid var(--text-color);
            padding: 20px;
            margin-bottom: 30px;
            position: relative;
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
        }
        
        .terminal::before {
            content: "> DECRYPTING DATA...";
            display: block;
            color: var(--text-color);
            margin-bottom: 10px;
            font-size: 0.9em;
            animation: blink 1s steps(1) infinite;
        }
        
        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }
        
        h2 {
            color: var(--accent-color);
            margin-bottom: 15px;
            font-size: 1.5em;
            position: relative;
            display: inline-block;
        }
        
        h2::after {
            content: "";
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 1px;
            background: linear-gradient(90deg, var(--accent-color), transparent);
        }
        
        p {
            margin-bottom: 15px;
            line-height: 1.6;
        }
        
        .highlight {
            color: var(--accent-color);
            font-weight: bold;
        }
        
        .code-block {
            background: rgba(0, 0, 0, 0.5);
            padding: 15px;
            border-left: 3px solid var(--accent-color);
            margin: 20px 0;
            overflow: auto;
        }
        
        .number-highlight {
            color: #ffcc00;
            font-weight: bold;
            font-size: 1.2em;
        }
        
        .socials {
            margin: 30px 0;
        }
        
        .socials a {
            display: inline-block;
            color: var(--text-color);
            text-decoration: none;
            margin-right: 20px;
            padding: 8px 15px;
            border: 1px solid var(--text-color);
            transition: all 0.3s ease;
        }
        
        .socials a:hover {
            background-color: var(--text-color);
            color: var(--main-bg);
        }
        
        .contract-info {
            margin: 30px 0;
            padding: 15px;
            border: 1px dashed var(--text-color);
            background: rgba(0, 255, 65, 0.05);
        }
        
        .conspiracy-matrix {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }
        
        .conspiracy-item {
            background: rgba(0, 0, 0, 0.7);
            padding: 15px;
            border: 1px solid var(--secondary-color);
            position: relative;
            overflow: hidden;
        }
        
        .conspiracy-item::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 5px;
            height: 100%;
            background: var(--secondary-color);
        }
        
        footer {
            text-align: center;
            padding: 30px 0;
            border-top: 1px solid rgba(0, 255, 65, 0.3);
            margin-top: 50px;
            font-size: 0.8em;
        }
        
        .countdown {
            font-size: 2em;
            text-align: center;
            margin: 30px 0;
            letter-spacing: 5px;
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }
        
        .hidden-message {
            text-align: center;
            margin: 20px 0;
            font-size: 0.8em;
            color: rgba(0, 255, 65, 0.5);
            cursor: help;
        }
        
        .hidden-message:hover {
            color: var(--accent-color);
        }
    </style>
</head>
<body>
    <div class="noise"></div>
    <div class="scanline"></div>
    <canvas class="matrix-effect" id="matrix"></canvas>
    
    <div class="container">
        <header>
            <h1 class="logo">R I D D L E</h1>
            <p class="tagline flicker">THE TRUTH IS HIDDEN IN PLAIN SIGHT</p>
            <div class="countdown" id="countdown">5 8 9</div>
        </header>
        
        <section class="terminal">
            <h2>The Masonic Plan Revealed</h2>
            <p>Ripple is all about the Riddle. Behind the veil of mainstream finance lies a carefully orchestrated plan, set in motion long before the world was ready to understand.</p>
            <p>One of the more enigmatic figures in the XRP community is <span class="highlight">Bearable Guy 123</span>, an account known for posting cryptic cartoons that many interpret as clues about the future of XRP. These are not mere coincidences - they are <span class="highlight">breadcrumbs</span> leading to the truth.</p>
            <p>The connection to Freemasons and the idea of an orchestrated plan from the beginning are part of a broader narrative within the community. This narrative frames the developments around XRP as part of a larger, premeditated strategy, with ties to influential figures and secret societies.</p>
            <div class="code-block">
                "The storm is coming" = <span class="number-highlight">589</span> in Jewish Gematria<br>
                "Brad Garlinghouse" + "Donald Trump" = <span class="number-highlight">589</span><br>
                "MATRIX" = <span class="number-highlight">58</span> + <span class="number-highlight">9</span> = 67
            </div>
        </section>
        
        <section class="terminal">
            <h2>The XRP Community: From Bearable Guy 123 to Elon Musk</h2>
            <p>A recent point of discussion has been a picture posted by Brad Garlinghouse, referencing a dinner with Donald Trump and the legal team of Ripple, amidst stormy seas and green candles. This imagery draws parallels to Bearable Guy 123's earlier posts, further fueling speculation about what these posts signify.</p>
            <p>Bearable Guy 123 has been particularly active on <span class="highlight">Christmas Eve</span>, a detail that has not gone unnoticed by the XRP community. This account is followed by Brad Garlinghouse, the CEO of Ripple, adding a layer of intrigue to the posts.</p>
            <p>The number <span class="number-highlight">589</span>, frequently mentioned in connection with Brad Garlinghouse, Donald Trump, and the term "storm is coming," resonates strongly within the community. This number and its associated terms in the Jewish gematria calculator hint at a coordinated message, especially when linked to well-known cultural references like "The Matrix."</p>
        </section>
        
        <div class="conspiracy-matrix">
            <div class="conspiracy-item">
                <h3>Christmas Eve Posts</h3>
                <p>Bearable Guy 123's activity peaks on December 24th. What message is being conveyed on this specific date?</p>
            </div>
            <div class="conspiracy-item">
                <h3>The Number 589</h3>
                <p>Not just a number, but a code. XRP price prediction or something more profound?</p>
            </div>
            <div class="conspiracy-item">
                <h3>The CEO Connection</h3>
                <p>Brad Garlinghouse follows BG123. Coincidence or confirmation?</p>
            </div>
            <div class="conspiracy-item">
                <h3>Trump & Ripple</h3>
                <p>The dinner, the storm, the green candles - all part of the plan.</p>
            </div>
        </div>
        
        <div class="hidden-message">
            "The key is hidden in the pattern of their communications."
        </div>
        
        <section class="contract-info terminal pulse">
            <h2>$RIDDLE Token Information</h2>
            <p><span class="highlight">Contract:</span> rBCNMMKxE4uiQ8ePZvyiL6DVKmeUZLBTJS</p>
            <p><span class="highlight">Initial Supply:</span> 580M (Dev burned tokens)</p>
            <p><span class="highlight">Chart:</span> <a href="https://dexscreener.com/xrpl/524944444c457800000000000000000000000000.rbcnmmkxe4uiq8epzvyil6dvkmeuzlbtjs_xrp" target="_blank" style="color: var(--text-color);">https://dexscreener.com/xrpl/524944444c457800000000000000000000000000.rbcnmmkxe4uiq8epzvyil6dvkmeuzlbtjs_xrp</a></p>
        </section>
        
        <div class="socials">
            <a href="https://t.me/riddle_ripple" target="_blank">JOIN TELEGRAM</a>
            <a href="https://x.com/RiddleOnXRP" target="_blank">FOLLOW ON X</a>
        </div>
        
        <footer>
            <p>"The truth will set you free, but first it will make you uncomfortable." - Unknown</p>
            <p class="hidden-message">Only those who can see beyond the veil will understand what's coming.</p>
        </footer>
    </div>
    
    <script>
        // Matrix effect
        const canvas = document.getElementById('matrix');
        const ctx = canvas.getContext('2d');
        
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        
        const symbols = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789$#@%&*()!?><abcdefghijklmnopqrstuvwxyz';
        const fontSize = 14;
        const columns = canvas.width / fontSize;
        
        const drops = [];
        for (let i = 0; i < columns; i++) {
            drops[i] = Math.floor(Math.random() * canvas.height);
        }
        
        function drawMatrix() {
            ctx.fillStyle = 'rgba(0, 0, 0, 0.05)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            
            ctx.fillStyle = '#00ff41';
            ctx.font = `${fontSize}px monospace`;
            
            for (let i = 0; i < drops.length; i++) {
                const text = symbols.charAt(Math.floor(Math.random() * symbols.length));
                ctx.fillText(text, i * fontSize, drops[i] * fontSize);
                
                if (drops[i] * fontSize > canvas.height && Math.random() > 0.975) {
                    drops[i] = 0;
                }
                
                drops[i]++;
            }
        }
        
        setInterval(drawMatrix, 33);
        
        // Glitch effect for countdown
        const countdownElement = document.getElementById('countdown');
        setInterval(() => {
            if (Math.random() > 0.9) {
                countdownElement.style.transform = `translate(${(Math.random() * 5) - 2.5}px, ${(Math.random() * 5) - 2.5}px)`;
                setTimeout(() => {
                    countdownElement.style.transform = 'translate(0, 0)';
                }, 100);
            }
        }, 2000);
        
        // Add hidden clues throughout the page
        document.addEventListener('DOMContentLoaded', () => {
            // Add hidden message in console
            console.log("%c                                                                    ", "background: url('data:image/svg+xml;utf8,<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"200\" height=\"20\"><text x=\"0\" y=\"15\" font-size=\"15\" fill=\"%2300ff41\">THE RIDDLE RUNS DEEPER</text></svg>'); background-repeat: no-repeat; font-size: 15px;");
            console.log("%cYou've found the first clue. There are 5 more hidden throughout the site.", "color: #00ff41; font-family: monospace;");
        });
        
        // Easter egg - hidden message appears on Konami code
        let konamiIndex = 0;
        const konamiCode = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65]; // up up down down left right left right B A
        
        document.addEventListener('keydown', (e) => {
            if (e.keyCode === konamiCode[konamiIndex]) {
                konamiIndex++;
                if (konamiIndex === konamiCode.length) {
                    const hiddenDiv = document.createElement('div');
                    hiddenDiv.style.position = 'fixed';
                    hiddenDiv.style.top = '50%';
                    hiddenDiv.style.left = '50%';
                    hiddenDiv.style.transform = 'translate(-50%, -50%)';
                    hiddenDiv.style.background = 'rgba(0, 0, 0, 0.9)';
                    hiddenDiv.style.padding = '20px';
                    hiddenDiv.style.zIndex = '1000';
                    hiddenDiv.style.border = '1px solid #00ff41';
                    hiddenDiv.innerHTML = `
                        <h3 style="color: #ff0044; margin-bottom: 10px;">Congratulations, Seeker</h3>
                        <p style="color: #00ff41;">The first key is: BEARABLE</p>
                        <p style="color: #00ff41;">Find the remaining keys to unlock the truth.</p>
                        <button id="close-hidden" style="background: none; border: 1px solid #00ff41; color: #00ff41; padding: 5px 10px; margin-top: 10px; cursor: pointer;">Close</button>
                    `;
                    document.body.appendChild(hiddenDiv);
                    
                    document.getElementById('close-hidden').addEventListener('click', () => {
                        document.body.removeChild(hiddenDiv);
                    });
                    
                    konamiIndex = 0;
                }
            } else {
                konamiIndex = 0;
            }
        });
    </script>
</body>
</html>
